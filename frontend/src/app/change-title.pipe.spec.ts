import { ChangeTitlePipe } from './change-title.pipe';

describe('ChangeTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
